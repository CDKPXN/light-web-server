<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.dao.MessageMapper">
  <resultMap id="BaseResultMap" type="com.company.project.model.Message">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="fromid" jdbcType="INTEGER" property="fromid" />
    <result column="toid" jdbcType="INTEGER" property="toid" />
    <result column="content" jdbcType="VARCHAR" property="content" />
    <result column="ctime" jdbcType="TIMESTAMP" property="ctime" />
  </resultMap>
  
 <select id="findHistory" resultMap="BaseResultMap" parameterType="Integer">
         select 
               id,fromid,toid,content,ctime
         from 
               message
         where
               (fromid = #{fromid} and toid = #{toid}) or (fromid = #{toid} and toid = #{fromid})
               and DATE_SUB(CURDATE(), INTERVAL 6 DAY) &lt;= ctime
         order by
                ctime
 
 </select>
 
 <select id="findToIdsById" resultType="Integer" parameterType="Integer">
   		  select distinct toid from message where fromid = #{fromid}
 </select>
 
 <select id="findFromIdsById" resultType="Integer" parameterType="Integer">
 		 select distinct fromid from message where toid = #{fromid}
 </select>
         
 <select id="findFirst" resultMap="BaseResultMap" parameterType="Integer">
         select 
               *
         from 
               message
         where
               (fromid = #{fromid} and toid = #{toid}) or (fromid = #{toid} and toid = #{fromid})
               and DATE_SUB(CURDATE(), INTERVAL 6 DAY) &lt;= ctime
         order by
                ctime desc
         limit 1
 
 </select>
 
 <select id="findRealnameByToid" resultType="String" parameterType="Integer">
         select realname from user where id = #{toid}
 </select>
</mapper>